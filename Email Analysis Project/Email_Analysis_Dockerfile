# Dockerfile to containerize Python application
FROM python:3.8-slim
WORKDIR /app
ADD . /app
RUN pip install --trusted-host pypi.python.org -r requirements.txt
EXPOSE 8501
ENV NAME World
CMD ["streamlit", "run", "your_app.py"]
